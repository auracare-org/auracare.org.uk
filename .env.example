# Resend API key for the investor contact form (/investors → /api/investor-contact).
# Create one at https://resend.com after verifying the auracare.org.uk domain,
# and set the same variable in Vercel → Project → Settings → Environment Variables
# (Production + Preview). Without it the form returns a friendly "not connected" error.
RESEND_API_KEY=
